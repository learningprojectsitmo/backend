# Pydantic schemas

Keep schemas consistent, think about inheritance, do not make too many of them

# SQLAlchemy models

Use "id" attribute instead of "user_id" to avoid "user.user_id"
Use self-expainatory attribute names, eg "author" instead of "user", so that we can say "project.author"
