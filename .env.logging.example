# Пример конфигурации логирования
# Скопируйте этот файл в .env и настройте по вашим потребностям

# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Формат логов
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Файл для основных логов
LOG_FILE=app.log

# Включить файловые логи (true/false)
ENABLE_FILE_LOGGING=true

# Включить консольные логи (true/false)
ENABLE_CONSOLE_LOGGING=true
