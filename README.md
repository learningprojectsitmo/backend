In order to run the backend:
1. Install **uv** on your system
1. cd into the backend repository, run `uv sync` - the project dependencies will be installed in a virtual environment
1. `uv run main.py` will run the server, it will be available at *localhost:8000*
