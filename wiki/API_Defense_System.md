# API Документация: Система записи на защиту проектов

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Роли и права доступа](#роли-и-права-доступа)
3. [Базовые концепции](#базовые-концепции)
4. [API Эндпоинты](#api-эндпоинты)
   - [Типы проектов](#типы-проектов)
   - [Дни защиты](#дни-защиты)
   - [Слоты защиты](#слоты-защиты)
   - [Запись на защиту](#запись-на-защиту)
5. [Схемы данных](#схемы-данных)
6. [Сценарии использования](#сценарии-использования)
7. [Коды ошибок](#коды-ошибок)

---

## 🎯 Обзор системы

Система позволяет:
- **Преподавателям**: создавать типы проектов, дни защиты и временные слоты
- **Студентам**: просматривать доступные слоты и записываться на защиту

### Архитектура системы

```
DefenseProjectType (Тип проекта)
         ↓
DefenseDay (День защиты) → DefenseSlot (Слот 30 мин) → DefenseRegistration (Запись студента)
```

---

## 🔐 Роли и права доступа

### Преподаватель (`role: "teacher"`)

✅ **Может:**
- Создавать типы проектов
- Создавать дни защиты
- Создавать слоты защиты
- Просматривать все списки

❌ **Не может:**
- Записываться на защиту (опционально, зависит от бизнес-логики)

### Студент (`role: "student"`)

✅ **Может:**
- Просматривать типы проектов
- Просматривать дни защиты
- Просматривать слоты защиты (с фильтрами)
- Записываться на слоты защиты

❌ **Не может:**
- Создавать типы проектов, дни или слоты

---

## 📚 Базовые концепции

### 1. Project Type (Тип проекта)

**Что это:** Категория проекта для защиты.

**Примеры:**
- "course" — Курсовая работа
- "lab" — Лабораторная работа
- "diploma" — Дипломная работа

**Создается:** Преподавателем  
**Используется:** Для фильтрации слотов по типу проекта

---

### 2. Defense Day (День защиты)

**Что это:** Конкретная дата проведения защит.

**Содержит:**
- `date` — Дата защиты (YYYY-MM-DD)
- `max_slots` — Максимальное количество слотов (например, 10)
- `first_slot_time` — Время первого слота (HH:MM:SS)

**Как работает:**
- Преподаватель создает день: "15 марта 2026, 10 слотов, начало в 10:00"
- Система автоматически вычисляет время всех слотов:
  - Слот 0: 10:00-10:30
  - Слот 1: 10:30-11:00
  - Слот 2: 11:00-11:30
  - ... до слота 9

---

### 3. Defense Slot (Слот защиты)

**Что это:** Временное окно для защиты проектов.

**Ключевые поля:**
- `defense_day_id` — ID дня защиты
- `slot_index` — Номер слота (0 до max_slots-1)
- `project_type_id` — ID типа проекта
- `title` — Название (например, "Защита курсовых работ")
- `location` — Аудитория (опционально)
- `capacity` — Количество мест (например, 5)
- `start_at` / `end_at` — Вычисляется автоматически

**Важно:**
- Время слота вычисляется: `first_slot_time + (slot_index × 30 минут)`
- Каждый слот длится ровно 30 минут
- На один слот можно записать несколько студентов (до `capacity`)

---

### 4. Defense Registration (Запись на защиту)

**Что это:** Факт записи студента на конкретный слот.

**Бизнес-правила:**
1. ✅ Студент может записаться только на существующий слот
2. ✅ Студент НЕ может записаться дважды на один и тот же слот
3. ✅ Количество записей не может превысить `capacity` слота
4. ✅ Запись создается с текущей датой и временем

**Важно:** Сам слот при записи студента НЕ изменяется. Система подсчитывает количество записей при каждом запросе.

---

## 🚀 API Эндпоинты

**Base URL:** `http://localhost:8000/v1/defense`

### Типы проектов

#### 1. Получить все типы проектов

```http
GET /v1/defense/project-types
```

**Авторизация:** Требуется (любая роль)

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "name": "course",
      "description": "Курсовая работа",
      "created_at": "2026-02-10T12:00:00Z"
    },
    {
      "id": 2,
      "name": "lab",
      "description": "Лабораторная работа",
      "created_at": "2026-02-10T12:05:00Z"
    }
  ]
}
```

**Пример (JavaScript):**
```javascript
const response = await fetch('http://localhost:8000/v1/defense/project-types', {
  headers: {
    'Authorization': `Bearer ${accessToken}`
  }
});
const data = await response.json();
```

---

#### 2. Создать тип проекта

```http
POST /v1/defense/project-types
```

**Авторизация:** Требуется (только преподаватель)

**Тело запроса:**
```json
{
  "name": "course",
  "description": "Курсовая работа"
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "name": "course",
  "description": "Курсовая работа",
  "created_at": "2026-02-10T12:00:00Z"
}
```

**Ошибки:**
- `400` — Тип проекта с таким названием уже существует
- `401` — Не авторизован
- `403` — Недостаточно прав (не преподаватель)

**Пример (JavaScript):**
```javascript
const response = await fetch('http://localhost:8000/v1/defense/project-types', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${accessToken}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'course',
    description: 'Курсовая работа'
  })
});
```

---

### Дни защиты

#### 3. Получить список дней защиты

```http
GET /v1/defense/days?page=1&limit=10
```

**Авторизация:** Требуется (любая роль)

**Query параметры:**
- `page` (integer, default=1) — Номер страницы
- `limit` (integer, default=10, max=100) — Количество элементов на странице

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "date": "2026-03-15",
      "max_slots": 10,
      "first_slot_time": "10:00:00",
      "created_at": "2026-02-10T12:00:00Z",
      "updated_at": "2026-02-10T12:00:00Z"
    }
  ],
  "total": 5,
  "page": 1,
  "limit": 10,
  "total_pages": 1
}
```

---

#### 4. Получить день защиты по ID

```http
GET /v1/defense/days/{day_id}
```

**Авторизация:** Требуется (любая роль)

**Ответ:**
```json
{
  "id": 1,
  "date": "2026-03-15",
  "max_slots": 10,
  "first_slot_time": "10:00:00",
  "created_at": "2026-02-10T12:00:00Z",
  "updated_at": "2026-02-10T12:00:00Z"
}
```

**Ошибки:**
- `404` — День не найден

---

#### 5. Создать день защиты

```http
POST /v1/defense/days
```

**Авторизация:** Требуется (только преподаватель)

**Тело запроса:**
```json
{
  "date": "2026-03-15",
  "max_slots": 10,
  "first_slot_time": "10:00:00"
}
```

**Ответ (201 Created):**
```json
{
  "id": 1,
  "date": "2026-03-15",
  "max_slots": 10,
  "first_slot_time": "10:00:00",
  "created_at": "2026-02-10T12:00:00Z",
  "updated_at": "2026-02-10T12:00:00Z"
}
```

**Ошибки:**
- `401` — Не авторизован
- `403` — Недостаточно прав (не преподаватель)
- `422` — Неверный формат данных

---

### Слоты защиты

#### 6. Получить список слотов

```http
GET /v1/defense/slots?page=1&limit=10&date=2026-03-15&project_type_id=1
```

**Авторизация:** Требуется (любая роль)

**Query параметры:**
- `page` (integer, default=1) — Номер страницы
- `limit` (integer, default=10, max=100) — Количество элементов
- `date` (date, optional) — Фильтр по дате (YYYY-MM-DD)
- `project_type_id` (integer, optional) — Фильтр по типу проекта

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "defense_day_id": 1,
      "slot_index": 0,
      "title": "Защита курсовых работ",
      "project_type": {
        "id": 1,
        "name": "course"
      },
      "start_at": "2026-03-15T10:00:00+00:00",
      "end_at": "2026-03-15T10:30:00+00:00",
      "location": "Аудитория 101",
      "capacity": 5
    },
    {
      "id": 2,
      "defense_day_id": 1,
      "slot_index": 1,
      "title": "Защита курсовых работ",
      "project_type": {
        "id": 1,
        "name": "course"
      },
      "start_at": "2026-03-15T10:30:00+00:00",
      "end_at": "2026-03-15T11:00:00+00:00",
      "location": "Аудитория 101",
      "capacity": 5
    }
  ],
  "total": 15,
  "page": 1,
  "limit": 10,
  "total_pages": 2
}
```

**Примеры фильтрации:**

```javascript
// Все слоты
fetch('/v1/defense/slots')

// Только слоты на 15 марта
fetch('/v1/defense/slots?date=2026-03-15')

// Только курсовые работы
fetch('/v1/defense/slots?project_type_id=1')

// Курсовые на 15 марта
fetch('/v1/defense/slots?date=2026-03-15&project_type_id=1')
```

---

#### 7. Получить слот по ID

```http
GET /v1/defense/slots/{slot_id}
```

**Авторизация:** Требуется (любая роль)

**Ответ:**
```json
{
  "id": 1,
  "defense_day_id": 1,
  "slot_index": 0,
  "title": "Защита курсовых работ",
  "project_type": {
    "id": 1,
    "name": "course"
  },
  "start_at": "2026-03-15T10:00:00+00:00",
  "end_at": "2026-03-15T10:30:00+00:00",
  "location": "Аудитория 101",
  "capacity": 5,
  "created_at": "2026-02-10T12:00:00Z",
  "updated_at": "2026-02-10T12:00:00Z"
}
```

**Ошибки:**
- `404` — Слот не найден

---

#### 8. Создать слот

```http
POST /v1/defense/slots
```

**Авторизация:** Требуется (только преподаватель)

**Тело запроса:**
```json
{
  "defense_day_id": 1,
  "slot_index": 0,
  "title": "Защита курсовых работ",
  "project_type_id": 1,
  "location": "Аудитория 101",
  "capacity": 5
}
```

**Важно:**
- `slot_index` должен быть от 0 до `max_slots - 1`
- Время `start_at` и `end_at` вычисляется автоматически
- Нельзя создать два слота с одинаковым `defense_day_id` и `slot_index`

**Ответ (201 Created):**
```json
{
  "id": 1,
  "defense_day_id": 1,
  "slot_index": 0,
  "title": "Защита курсовых работ",
  "project_type": {
    "id": 1,
    "name": "course"
  },
  "start_at": "2026-03-15T10:00:00+00:00",
  "end_at": "2026-03-15T10:30:00+00:00",
  "location": "Аудитория 101",
  "capacity": 5,
  "created_at": "2026-02-10T12:00:00Z",
  "updated_at": "2026-02-10T12:00:00Z"
}
```

**Ошибки:**
- `400` — slot_index вне диапазона, или слот уже существует
- `401` — Не авторизован
- `403` — Недостаточно прав (не преподаватель)
- `404` — День защиты или тип проекта не найден

---

### Запись на защиту

#### 9. Записаться на слот

```http
POST /v1/defense/slots/{slot_id}/register
```

**Авторизация:** Требуется (студент или преподаватель)

**Тело запроса:**
```json
{}
```
*(пустой объект, user_id берется из токена)*

**Ответ (201 Created):**
```json
{
  "id": 1,
  "slot_id": 1,
  "user_id": 42,
  "created_at": "2026-02-10T15:30:00Z"
}
```

**Бизнес-правила:**
1. Слот должен существовать
2. Пользователь не может записаться дважды на один слот
3. Количество записей не должно превышать `capacity` слота

**Ошибки:**
- `400` — Слот заполнен ("Defense slot is full")
- `400` — Пользователь уже записан ("User is already registered for this defense slot")
- `401` — Не авторизован
- `404` — Слот не найден

**Пример (JavaScript):**
```javascript
const response = await fetch(`http://localhost:8000/v1/defense/slots/${slotId}/register`, {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${accessToken}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({})
});

if (response.ok) {
  alert('Вы успешно записались на защиту!');
} else if (response.status === 400) {
  const error = await response.json();
  alert(error.detail); // "Defense slot is full" или "User is already registered..."
}
```

---

## 📊 Схемы данных

### ProjectTypeCreate (Создание типа проекта)

```typescript
{
  name: string;           // Уникальное название
  description?: string;   // Описание (опционально)
}
```

### ProjectTypeFull (Полный тип проекта)

```typescript
{
  id: number;
  name: string;
  description: string | null;
  created_at: string; // ISO 8601
}
```

---

### DefenseDayCreate (Создание дня защиты)

```typescript
{
  date: string;           // YYYY-MM-DD
  max_slots: number;      // Количество слотов (например, 10)
  first_slot_time: string; // HH:MM:SS (например, "10:00:00")
}
```

### DefenseDayFull (Полный день защиты)

```typescript
{
  id: number;
  date: string;           // YYYY-MM-DD
  max_slots: number;
  first_slot_time: string; // HH:MM:SS
  created_at: string;     // ISO 8601
  updated_at: string;     // ISO 8601
}
```

---

### DefenseSlotCreate (Создание слота)

```typescript
{
  defense_day_id: number;    // ID дня защиты
  slot_index: number;        // Номер слота (0 до max_slots-1)
  title: string;             // Название
  project_type_id: number;   // ID типа проекта
  location?: string;         // Аудитория (опционально)
  capacity: number;          // Вместимость
}
```

### DefenseSlotFull (Полный слот)

```typescript
{
  id: number;
  defense_day_id: number;
  slot_index: number;
  title: string;
  project_type: {            // Вложенный объект
    id: number;
    name: string;
  };
  start_at: string;          // ISO 8601 с timezone
  end_at: string;            // ISO 8601 с timezone
  location: string | null;
  capacity: number;
  created_at: string;
  updated_at: string;
}
```

---

### DefenseRegistrationFull (Запись на защиту)

```typescript
{
  id: number;
  slot_id: number;
  user_id: number;
  created_at: string; // ISO 8601
}
```

---

## 🎬 Сценарии использования

### Сценарий 1: Преподаватель создает расписание

```javascript
// 1. Создать тип проекта
const projectType = await fetch('/v1/defense/project-types', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${teacherToken}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'course',
    description: 'Курсовая работа'
  })
}).then(r => r.json());

console.log('Создан тип:', projectType.id); // id: 1

// 2. Создать день защиты
const day = await fetch('/v1/defense/days', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${teacherToken}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    date: '2026-03-15',
    max_slots: 10,
    first_slot_time: '10:00:00'
  })
}).then(r => r.json());

console.log('Создан день:', day.id); // id: 1

// 3. Создать несколько слотов
for (let i = 0; i < 3; i++) {
  const slot = await fetch('/v1/defense/slots', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${teacherToken}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      defense_day_id: day.id,
      slot_index: i,
      title: 'Защита курсовых работ',
      project_type_id: projectType.id,
      location: 'Аудитория 101',
      capacity: 5
    })
  }).then(r => r.json());
  
  console.log(`Слот ${i}:`, slot.start_at, '-', slot.end_at);
}
// Слот 0: 2026-03-15T10:00:00+00:00 - 2026-03-15T10:30:00+00:00
// Слот 1: 2026-03-15T10:30:00+00:00 - 2026-03-15T11:00:00+00:00
// Слот 2: 2026-03-15T11:00:00+00:00 - 2026-03-15T11:30:00+00:00
```

---

### Сценарий 2: Студент просматривает и записывается на слот

```javascript
// 1. Получить все типы проектов
const types = await fetch('/v1/defense/project-types', {
  headers: { 'Authorization': `Bearer ${studentToken}` }
}).then(r => r.json());

console.log('Доступные типы:', types.items);

// 2. Получить слоты для курсовых на 15 марта
const slots = await fetch('/v1/defense/slots?date=2026-03-15&project_type_id=1', {
  headers: { 'Authorization': `Bearer ${studentToken}` }
}).then(r => r.json());

console.log('Найдено слотов:', slots.total);

// 3. Показать информацию о первом слоте
const firstSlot = slots.items[0];
console.log('Слот:', firstSlot.title);
console.log('Время:', new Date(firstSlot.start_at).toLocaleString());
console.log('Аудитория:', firstSlot.location);
console.log('Мест:', firstSlot.capacity);

// 4. Записаться на слот
try {
  const registration = await fetch(`/v1/defense/slots/${firstSlot.id}/register`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${studentToken}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({})
  }).then(r => {
    if (!r.ok) throw new Error(r.statusText);
    return r.json();
  });
  
  console.log('✅ Вы записаны на защиту!');
  console.log('ID записи:', registration.id);
} catch (error) {
  console.error('❌ Ошибка:', error.message);
}
```

---

### Сценарий 3: Проверка доступности слота перед записью

```javascript
async function checkSlotAvailability(slotId, token) {
  // Получить информацию о слоте
  const slot = await fetch(`/v1/defense/slots/${slotId}`, {
    headers: { 'Authorization': `Bearer ${token}` }
  }).then(r => r.json());
  
  // TODO: В будущем можно добавить эндпоинт для получения
  // текущего количества записей на слот
  // Сейчас эта информация не доступна через API
  
  console.log(`Слот: ${slot.title}`);
  console.log(`Время: ${new Date(slot.start_at).toLocaleString()}`);
  console.log(`Вместимость: ${slot.capacity} мест`);
  
  return slot;
}

// Использование
const slot = await checkSlotAvailability(1, studentToken);
```

---

### Сценарий 4: Фильтрация слотов по критериям

```javascript
async function findSuitableSlots(criteria, token) {
  const params = new URLSearchParams({
    page: '1',
    limit: '100'
  });
  
  if (criteria.date) {
    params.append('date', criteria.date);
  }
  
  if (criteria.projectTypeId) {
    params.append('project_type_id', criteria.projectTypeId.toString());
  }
  
  const response = await fetch(`/v1/defense/slots?${params}`, {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  
  const data = await response.json();
  return data.items;
}

// Примеры использования:

// Все курсовые на 15 марта
const courseSlots = await findSuitableSlots({
  date: '2026-03-15',
  projectTypeId: 1
}, studentToken);

// Все слоты на конкретную дату
const allSlotsOnDate = await findSuitableSlots({
  date: '2026-03-15'
}, studentToken);

// Все лабораторные (любая дата)
const labSlots = await findSuitableSlots({
  projectTypeId: 2
}, studentToken);
```

---

## ⚠️ Коды ошибок

### 400 Bad Request
**Причины:**
- Слот заполнен: `"Defense slot is full"`
- Пользователь уже записан: `"User is already registered for this defense slot"`
- `slot_index` вне диапазона: `"slot_index must be in [0, 9], got 15"`
- Слот уже существует: `"Slot with index 2 already exists for this day"`
- Тип проекта с таким именем существует: `"Project type with name 'course' already exists"`

**Решение:** Проверить входные данные, выбрать другой слот

---

### 401 Unauthorized
**Причина:** Не передан токен или токен недействителен

**Решение:**
```javascript
// Убедитесь что передаете токен
headers: {
  'Authorization': `Bearer ${accessToken}`
}
```

---

### 403 Forbidden
**Причина:** Недостаточно прав (пытается выполнить действие преподавателя будучи студентом)

**Сообщение:** `"Only teachers can perform this action"`

**Решение:** Проверить роль пользователя перед выполнением действия

```javascript
if (user.role === 'teacher') {
  // Показать кнопки создания
} else {
  // Показать только просмотр и запись
}
```

---

### 404 Not Found
**Причины:**
- День защиты не найден: `"Defense day not found"`
- Слот не найден: `"Defense slot not found"`
- Тип проекта не найден: `"Project type not found"`

**Решение:** Проверить существование ресурса перед обращением

---

### 422 Unprocessable Entity
**Причина:** Неверный формат данных в запросе

**Пример ошибки:**
```json
{
  "detail": [
    {
      "loc": ["body", "date"],
      "msg": "invalid date format",
      "type": "value_error.date"
    }
  ]
}
```

**Решение:** Проверить типы и формат полей согласно схемам данных

---

## 💡 Рекомендации для фронтенда

### 1. Кэширование типов проектов
Типы проектов редко меняются, можно кэшировать на стороне клиента:

```javascript
let projectTypesCache = null;

async function getProjectTypes(token, forceRefresh = false) {
  if (!forceRefresh && projectTypesCache) {
    return projectTypesCache;
  }
  
  const response = await fetch('/v1/defense/project-types', {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  
  projectTypesCache = await response.json();
  return projectTypesCache;
}
```

---

### 2. Обработка ошибок
Создайте универсальную функцию для обработки ошибок API:

```javascript
async function apiRequest(url, options) {
  try {
    const response = await fetch(url, options);
    
    if (!response.ok) {
      const error = await response.json();
      
      switch (response.status) {
        case 400:
          throw new Error(error.detail);
        case 401:
          // Перенаправить на логин
          window.location.href = '/login';
          break;
        case 403:
          throw new Error('Недостаточно прав для выполнения действия');
        case 404:
          throw new Error('Ресурс не найден');
        default:
          throw new Error('Произошла ошибка сервера');
      }
    }
    
    return await response.json();
  } catch (error) {
    console.error('API Error:', error);
    throw error;
  }
}
```

---

### 3. Отображение времени
Время в API всегда в UTC, конвертируйте в локальное время пользователя:

```javascript
function formatSlotTime(startAt, endAt) {
  const start = new Date(startAt);
  const end = new Date(endAt);
  
  const options = {
    hour: '2-digit',
    minute: '2-digit',
    timeZone: 'Europe/Moscow' // или браузерный timezone
  };
  
  return `${start.toLocaleTimeString('ru-RU', options)} - ${end.toLocaleTimeString('ru-RU', options)}`;
}

// Использование:
console.log(formatSlotTime(slot.start_at, slot.end_at));
// "10:00 - 10:30"
```

---

### 4. UI подсказки
Добавьте визуальные индикаторы:

```javascript
function getSlotStatus(slot, registrationsCount) {
  const percentage = (registrationsCount / slot.capacity) * 100;
  
  if (percentage >= 100) {
    return { status: 'full', color: 'red', text: 'Мест нет' };
  } else if (percentage >= 80) {
    return { status: 'almost-full', color: 'orange', text: 'Почти заполнен' };
  } else {
    return { status: 'available', color: 'green', text: 'Есть места' };
  }
}
```

---

## 📝 Дополнительная информация

### Лимиты API
- Максимум `limit` для пагинации: **100**
- Длина слота: **фиксированно 30 минут**
- Формат даты: **YYYY-MM-DD** (ISO 8601)
- Формат времени: **HH:MM:SS**

### Timezone
Все временные метки в API в **UTC** (ISO 8601 с `+00:00`). Конвертируйте в локальное время на клиенте.

### Версионирование
API версионируется через URL: `/v1/defense/...`

При изменении версии старая версия продолжит работать.

---

## 🔗 Связанные документы

- [API_Users_Auth.md](./API_Users_Auth.md) — Авторизация и управление пользователями
- [README.md](../README.md) — Основная документация проекта

---

**Дата создания:** 2026-02-14  
**Версия API:** v1  
**Автор:** Backend Team
